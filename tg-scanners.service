[Unit]
Description=TG Scanners Service
After=network.target
Wants=network-online.target

[Service]
Type=simple

# Run as dedicated user
User=tg-scanner
Group=tg-scanner

# There the code is cloned
WorkingDirectory=/opt/tg-scanners

# Env file saved here
EnvironmentFile=/etc/tg-scanners/tg-scanners.env

# Use the venv Python explicitly
ExecStart=/opt/tg-scanners/venv/bin/python -m tg_scanners.main --mode mock

# Environment (optional but recommended)
Environment=PYTHONUNBUFFERED=1

# Reliability
Restart=on-failure
RestartSec=5

# Logging
StandardOutput=journal
StandardError=journal

# Hardening (safe defaults)
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ProtectHome=true

[Install]
WantedBy=multi-user.target
